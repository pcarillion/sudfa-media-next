import { Container } from "@/components/common/Container";
import React from "react";

/**
 * Conteneur de confirmation après envoi de message de contact
 * @returns {Promise<JSX.Element>} Page de confirmation
 */
export const ContactConformationContainer = async () => {
  return <Container>Merci pour votre message</Container>;
};
